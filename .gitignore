node_modules
.next
tsconfig.tsbuildinfo
.env
.env.local
.env*.local
*.log
.DS_Store

# Videos are served from CDN — never commit binary files to git
public/videos/

# Pipeline intermediate files (local processing only)
pipeline/compressed/
pipeline/thumbnails/
pipeline/.env
pipeline/generation_status.json
